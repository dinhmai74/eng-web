import React, { Component } from 'react'
import styled from 'styled-components'
import { metrics } from 'themes'

const NavMargin = styled('div')`
  padding-top: ${metrics.navSize};
`

export default NavMargin
