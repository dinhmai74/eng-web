import React from "react";
import { storiesOf } from "@storybook/react";

import PreparationIelts from "../../containers/preparation-Ielts-screen/preparationIelts";

storiesOf("preparationIelts form", module).add("default", () => (
  <PreparationIelts />
));
